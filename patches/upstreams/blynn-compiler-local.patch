diff --git a/inn/RTS.hs b/inn/RTS.hs
index 812ccfa..434006b 100644
--- a/inn/RTS.hs
+++ b/inn/RTS.hs
@@ -36,62 +36,83 @@ void errexit() { fputc('\n', stderr); }
 preamble = [r|#define EXPORT(f, sym, n) void f() asm(sym) __attribute__((export_name(sym))); void f(){rts_reduce(root[n]);}
 void *malloc(unsigned long);
 enum { FORWARD = 127, REDUCING = 126 };
-enum { TOP = 1<<24 };
+enum { TOP = 16777216 };
 static u *mem, *altmem, *sp, *spTop, hp;
 static inline u isAddr(u n) { return n>=128; }
 static u evac(u n) {
   if (!isAddr(n)) return n;
-  u x = mem[n];
-  while (isAddr(x) && mem[x] == _T) {
-    mem[n] = mem[n + 1];
-    mem[n + 1] = mem[x + 1];
-    x = mem[n];
+  u *pn = mem + n * sizeof(u);
+  u x = *pn;
+  while (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px != _T) break;
+    u *pn1 = mem + (n + 1) * sizeof(u);
+    u *px1 = mem + (x + 1) * sizeof(u);
+    *pn = *pn1;
+    *pn1 = *px1;
+    x = *pn;
   }
-  if (isAddr(x) && mem[x] == _K) {
-    mem[n + 1] = mem[x + 1];
-    x = mem[n] = _I;
+  if (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px == _K) {
+      u *pn1 = mem + (n + 1) * sizeof(u);
+      u *px1 = mem + (x + 1) * sizeof(u);
+      *pn1 = *px1;
+      x = _I;
+      *pn = x;
+    }
   }
-  u y = mem[n + 1];
+  u *pn1 = mem + (n + 1) * sizeof(u);
+  u y = *pn1;
   switch(x) {
     case FORWARD: return y;
     case REDUCING:
-      mem[n] = FORWARD;
-      mem[n + 1] = hp;
+      *pn = FORWARD;
+      *pn1 = hp;
       hp += 2;
-      return mem[n + 1];
+      return *pn1;
     case _I:
-      mem[n] = REDUCING;
+      *pn = REDUCING;
       y = evac(y);
-      if (mem[n] == FORWARD) {
-        altmem[mem[n + 1]] = _I;
-        altmem[mem[n + 1] + 1] = y;
+      if (*pn == FORWARD) {
+        u *pa = altmem + *pn1 * sizeof(u);
+        *pa = _I;
+        pa = altmem + (*pn1 + 1) * sizeof(u);
+        *pa = y;
       } else {
-        mem[n] = FORWARD;
-        mem[n + 1] = y;
+        *pn = FORWARD;
+        *pn1 = y;
       }
-      return mem[n + 1];
+      return *pn1;
     default: break;
   }
   u z = hp;
   hp += 2;
-  mem[n] = FORWARD;
-  mem[n + 1] = z;
-  altmem[z] = x;
-  altmem[z + 1] = y;
+  *pn = FORWARD;
+  *pn1 = z;
+  u *pa = altmem + z * sizeof(u);
+  *pa = x;
+  pa = altmem + (z + 1) * sizeof(u);
+  *pa = y;
   return z;
 }
 
 static void gc() {
   hp = 128;
   u di = hp;
-  sp = altmem + TOP - 1;
-  for(u *r = root; *r; r++) *r = evac(*r);
+  sp = altmem + (TOP - 1) * sizeof(u);
+  for(u *r = root; *r; r = r + sizeof(u)) *r = evac(*r);
   *sp = evac(*spTop);
   while (di < hp) {
-    u x = altmem[di] = evac(altmem[di]);
-    di++;
-    if (x != _NUM) altmem[di] = evac(altmem[di]);
-    di++;
+    u *pd = altmem + di * sizeof(u);
+    u x = evac(*pd);
+    *pd = x;
+    di = di + 1;
+    if (x != _NUM) {
+      pd = altmem + di * sizeof(u);
+      *pd = evac(*pd);
+    }
+    di = di + 1;
   }
   spTop = sp;
   u *tmp = mem;
@@ -99,23 +120,50 @@ static void gc() {
   altmem = tmp;
 }
 
-static inline u app(u f, u x) { mem[hp] = f; mem[hp + 1] = x; return (hp += 2) - 2; }
-static inline u arg(u n) { return mem[sp [n] + 1]; }
-static inline int num(u n) { return mem[arg(n) + 1]; }
-static inline void lazy2(u height, u f, u x) {
-  u *p = mem + sp[height];
+static inline u app(u f, u x) {
+  u *p = mem + hp * sizeof(u);
   *p = f;
-  *++p = x;
-  sp += height - 1;
+  p = mem + (hp + 1) * sizeof(u);
+  *p = x;
+  hp += 2;
+  return hp - 2;
+}
+static inline u arg(u n) {
+  u *p = sp + n * sizeof(u);
+  p = mem + (*p + 1) * sizeof(u);
+  return *p;
+}
+static inline int num(u n) {
+  u a = arg(n);
+  u *p = mem + (a + 1) * sizeof(u);
+  return *p;
+}
+static inline void lazy2(u height, u f, u x) {
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  *p = f;
+  p = p + sizeof(u);
+  *p = x;
+  sp = sp + (height - 1) * sizeof(u);
   *sp = f;
 }
-static void lazy3(u height,u x1,u x2,u x3){u*p=mem+sp[height];sp[height-1]=*p=app(x1,x2);*++p=x3;*(sp+=height-2)=x1;}
+static void lazy3(u height,u x1,u x2,u x3){
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  u *ps = sp + (height - 1) * sizeof(u);
+  *ps = app(x1, x2);
+  *p = *ps;
+  p = p + sizeof(u);
+  *p = x3;
+  sp = sp + (height - 2) * sizeof(u);
+  *sp = x1;
+}
 |]
 
 -- Main VM loop.
 comdefsrc = [r|
 F x = "foreign(num(1));"
-Y x = x "sp[1]"
+Y x = "{u *p = sp + sizeof(u); lazy2(1, arg(1), *p);}"
 Q x y z = z(y x)
 S x y z = x z(y z)
 B x y z = x (y z)
@@ -126,23 +174,23 @@ V x y z = z x y
 T x y = y x
 K x y = "_I" x
 KI x y = "_I" y
-I x = "sp[1] = arg(1); sp++;"
+I x = "{u *p = sp + sizeof(u); *p = arg(1); sp = sp + sizeof(u);}"
 LEFT x y z = y x
 CONS x y z w = w x y
-NUM x y = y "sp[1]"
+NUM x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 ADD x y = "_NUM" "num(1) + num(2)"
 SUB x y = "_NUM" "num(1) - num(2)"
 MUL x y = "_NUM" "num(1) * num(2)"
 DIV x y = "_NUM" "num(1) / num(2)"
 MOD x y = "_NUM" "num(1) % num(2)"
-EQ x y = "num(1) == num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-LE x y = "num(1) <= num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-REF x y = y "sp[1]"
+EQ x y = "if (num(1) == num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+LE x y = "if (num(1) <= num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+REF x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
-WRITEREF x y z w = w "((mem[arg(2) + 1] = arg(1)), _K)" z
+WRITEREF x y z w = "{u *p = mem + (arg(2) + 1) * sizeof(u); *p = arg(1); lazy3(4, arg(4), _K, arg(3));}"
 END = "return;"
-ERR = "sp[1]=app(app(arg(1),_ERREND),_ERR2);sp++;"
+ERR = "{u *p = sp + sizeof(u); *p = app(app(arg(1), _ERREND), _ERR2); sp = sp + sizeof(u);}"
 ERR2 = "lazy3(2, arg(1), _ERROUT, arg(2));"
 ERROUT = "errchar(num(1)); lazy2(2, _ERR, arg(2));"
 ERREND = "errexit(); return;"
@@ -216,10 +264,10 @@ comEnum s = maybe (error s) id $ lookup s $ zip (fst <$> comdefs) [1..]
 comName i = maybe undefined id $ lookup i $ zip [1..] (fst <$> comdefs)
 
 runFun = ([r|static void run() {
-  for(;;) {
-    if (mem + hp > sp - 8) gc();
+  while(1) {
+    if (mem + hp * sizeof(u) > sp - 8 * sizeof(u)) gc();
     u x = *sp;
-    if (isAddr(x)) *--sp = mem[x]; else switch(x) {
+    if (isAddr(x)) { u *p = mem + x * sizeof(u); sp = sp - sizeof(u); *sp = *p; } else switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ([r|
@@ -230,13 +278,14 @@ runFun = ([r|static void run() {
 void rts_init() {
   mem = malloc(TOP * sizeof(u)); altmem = malloc(TOP * sizeof(u));
   hp = 128;
-  for (u i = 0; i < sizeof(prog)/sizeof(*prog); i++) mem[hp++] = prog[i];
-  spTop = mem + TOP - 1;
+  for (u i = 0; i < prog_size; i = i + 1) { u *p = mem + hp * sizeof(u); *p = prog[i]; hp = hp + 1; }
+  spTop = mem + (TOP - 1) * sizeof(u);
 }
 
 void rts_reduce(u n) {
   static u ready;if (!ready){ready=1;rts_init();}
-  *(sp = spTop) = app(app(n, _UNDEFINED), _END);
+  sp = spTop;
+  *sp = app(app(n, _UNDEFINED), _END);
   run();
 }
 |]++)
@@ -318,7 +367,7 @@ compile mods = do
     . ("};\n"++)
     . ("static const u prog[]={" ++)
     . foldr (.) id (map (\n -> shows n . (',':)) mem)
-    . ("};\nstatic u root[]={" ++)
+    . ("};\nstatic const u prog_size="++) . shows (length mem) . (";\nstatic u root[]={"++)
     . foldr (\(modName, (_, ourName)) f -> shows (resolve bigmap (modName, ourName)) . (", " ++) . f) id ffes
     . ("0};\n" ++)
     . (preamble++)
diff --git a/inn/RTS1.hs b/inn/RTS1.hs
index 8e8fc90..5cb6f9b 100644
--- a/inn/RTS1.hs
+++ b/inn/RTS1.hs
@@ -38,62 +38,83 @@ void errexit() { fputc('\n', stderr); }
 preamble = [r|#define EXPORT(f, sym) void f() asm(sym) __attribute__((export_name(sym)));
 void *malloc(unsigned long);
 enum { FORWARD = 127, REDUCING = 126 };
-enum { TOP = 1<<24 };
+enum { TOP = 16777216 };
 static u *mem, *altmem, *sp, *spTop, hp;
 static inline u isAddr(u n) { return n>=128; }
 static u evac(u n) {
   if (!isAddr(n)) return n;
-  u x = mem[n];
-  while (isAddr(x) && mem[x] == _T) {
-    mem[n] = mem[n + 1];
-    mem[n + 1] = mem[x + 1];
-    x = mem[n];
+  u *pn = mem + n * sizeof(u);
+  u x = *pn;
+  while (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px != _T) break;
+    u *pn1 = mem + (n + 1) * sizeof(u);
+    u *px1 = mem + (x + 1) * sizeof(u);
+    *pn = *pn1;
+    *pn1 = *px1;
+    x = *pn;
   }
-  if (isAddr(x) && mem[x] == _K) {
-    mem[n + 1] = mem[x + 1];
-    x = mem[n] = _I;
+  if (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px == _K) {
+      u *pn1 = mem + (n + 1) * sizeof(u);
+      u *px1 = mem + (x + 1) * sizeof(u);
+      *pn1 = *px1;
+      x = _I;
+      *pn = x;
+    }
   }
-  u y = mem[n + 1];
+  u *pn1 = mem + (n + 1) * sizeof(u);
+  u y = *pn1;
   switch(x) {
     case FORWARD: return y;
     case REDUCING:
-      mem[n] = FORWARD;
-      mem[n + 1] = hp;
+      *pn = FORWARD;
+      *pn1 = hp;
       hp += 2;
-      return mem[n + 1];
+      return *pn1;
     case _I:
-      mem[n] = REDUCING;
+      *pn = REDUCING;
       y = evac(y);
-      if (mem[n] == FORWARD) {
-        altmem[mem[n + 1]] = _I;
-        altmem[mem[n + 1] + 1] = y;
+      if (*pn == FORWARD) {
+        u *pa = altmem + *pn1 * sizeof(u);
+        *pa = _I;
+        pa = altmem + (*pn1 + 1) * sizeof(u);
+        *pa = y;
       } else {
-        mem[n] = FORWARD;
-        mem[n + 1] = y;
+        *pn = FORWARD;
+        *pn1 = y;
       }
-      return mem[n + 1];
+      return *pn1;
     default: break;
   }
   u z = hp;
   hp += 2;
-  mem[n] = FORWARD;
-  mem[n + 1] = z;
-  altmem[z] = x;
-  altmem[z + 1] = y;
+  *pn = FORWARD;
+  *pn1 = z;
+  u *pa = altmem + z * sizeof(u);
+  *pa = x;
+  pa = altmem + (z + 1) * sizeof(u);
+  *pa = y;
   return z;
 }
 
 static void gc() {
   hp = 128;
   u di = hp;
-  sp = altmem + TOP - 1;
-  for(u *r = root; *r; r++) *r = evac(*r);
+  sp = altmem + (TOP - 1) * sizeof(u);
+  for(u *r = root; *r; r = r + sizeof(u)) *r = evac(*r);
   *sp = evac(*spTop);
   while (di < hp) {
-    u x = altmem[di] = evac(altmem[di]);
-    di++;
-    if (x != _NUM) altmem[di] = evac(altmem[di]);
-    di++;
+    u *pd = altmem + di * sizeof(u);
+    u x = evac(*pd);
+    *pd = x;
+    di = di + 1;
+    if (x != _NUM) {
+      pd = altmem + di * sizeof(u);
+      *pd = evac(*pd);
+    }
+    di = di + 1;
   }
   spTop = sp;
   u *tmp = mem;
@@ -101,23 +122,50 @@ static void gc() {
   altmem = tmp;
 }
 
-static inline u app(u f, u x) { mem[hp] = f; mem[hp + 1] = x; return (hp += 2) - 2; }
-static inline u arg(u n) { return mem[sp [n] + 1]; }
-static inline int num(u n) { return mem[arg(n) + 1]; }
-static inline void lazy2(u height, u f, u x) {
-  u *p = mem + sp[height];
+static inline u app(u f, u x) {
+  u *p = mem + hp * sizeof(u);
   *p = f;
-  *++p = x;
-  sp += height - 1;
+  p = mem + (hp + 1) * sizeof(u);
+  *p = x;
+  hp += 2;
+  return hp - 2;
+}
+static inline u arg(u n) {
+  u *p = sp + n * sizeof(u);
+  p = mem + (*p + 1) * sizeof(u);
+  return *p;
+}
+static inline int num(u n) {
+  u a = arg(n);
+  u *p = mem + (a + 1) * sizeof(u);
+  return *p;
+}
+static inline void lazy2(u height, u f, u x) {
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  *p = f;
+  p = p + sizeof(u);
+  *p = x;
+  sp = sp + (height - 1) * sizeof(u);
   *sp = f;
 }
-static void lazy3(u height,u x1,u x2,u x3){u*p=mem+sp[height];sp[height-1]=*p=app(x1,x2);*++p=x3;*(sp+=height-2)=x1;}
+static void lazy3(u height,u x1,u x2,u x3){
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  u *ps = sp + (height - 1) * sizeof(u);
+  *ps = app(x1, x2);
+  *p = *ps;
+  p = p + sizeof(u);
+  *p = x3;
+  sp = sp + (height - 2) * sizeof(u);
+  *sp = x1;
+}
 |]
 
 -- Main VM loop.
 comdefsrc = [r|
 F x = "foreign(num(1));"
-Y x = x "sp[1]"
+Y x = "{u *p = sp + sizeof(u); lazy2(1, arg(1), *p);}"
 Q x y z = z(y x)
 S x y z = x z(y z)
 B x y z = x (y z)
@@ -128,10 +176,10 @@ V x y z = z x y
 T x y = y x
 K x y = "_I" x
 KI x y = "_I" y
-I x = "sp[1] = arg(1); sp++;"
+I x = "{u *p = sp + sizeof(u); *p = arg(1); sp = sp + sizeof(u);}"
 LEFT x y z = y x
 CONS x y z w = w x y
-NUM x y = y "sp[1]"
+NUM x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 ADD x y = "_NUM" "num(1) + num(2)"
 SUB x y = "_NUM" "num(1) - num(2)"
 MUL x y = "_NUM" "num(1) * num(2)"
@@ -142,14 +190,14 @@ MOD x y = "_NUM" "mod(num(1), num(2))"
 XOR x y = "_NUM" "num(1) ^ num(2)"
 AND x y = "_NUM" "num(1) & num(2)"
 OR x y = "_NUM" "num(1) | num(2)"
-EQ x y = "num(1) == num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-LE x y = "num(1) <= num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-REF x y = y "sp[1]"
+EQ x y = "if (num(1) == num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+LE x y = "if (num(1) <= num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+REF x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
-WRITEREF x y z w = w "((mem[arg(2) + 1] = arg(1)), _K)" z
+WRITEREF x y z w = "{u *p = mem + (arg(2) + 1) * sizeof(u); *p = arg(1); lazy3(4, arg(4), _K, arg(3));}"
 END = "return;"
-ERR = "sp[1]=app(app(arg(1),_ERREND),_ERR2);sp++;"
+ERR = "{u *p = sp + sizeof(u); *p = app(app(arg(1), _ERREND), _ERR2); sp = sp + sizeof(u);}"
 ERR2 = "lazy3(2, arg(1), _ERROUT, arg(2));"
 ERROUT = "errchar(num(1)); lazy2(2, _ERR, arg(2));"
 ERREND = "errexit(); return;"
@@ -232,10 +280,10 @@ static int div(int a, int b) { int q = a/b; return q - (((u)(a^b)) >> 31)*(q*b!=
 static int mod(int a, int b) { int r = a%b; return r + (((u)(a^b)) >> 31)*(!!r)*b; }
 
 static void run() {
-  for(;;) {
-    if (mem + hp > sp - 8) gc();
+  while(1) {
+    if (mem + hp * sizeof(u) > sp - 8 * sizeof(u)) gc();
     u x = *sp;
-    if (isAddr(x)) *--sp = mem[x]; else switch(x) {
+    if (isAddr(x)) { u *p = mem + x * sizeof(u); sp = sp - sizeof(u); *sp = *p; } else switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ([r|
@@ -246,13 +294,14 @@ static void run() {
 void rts_init() {
   mem = malloc(TOP * sizeof(u)); altmem = malloc(TOP * sizeof(u));
   hp = 128;
-  for (u i = 0; i < sizeof(prog)/sizeof(*prog); i++) mem[hp++] = prog[i];
-  spTop = mem + TOP - 1;
+  for (u i = 0; i < prog_size; i = i + 1) { u *p = mem + hp * sizeof(u); *p = prog[i]; hp = hp + 1; }
+  spTop = mem + (TOP - 1) * sizeof(u);
 }
 
 void rts_reduce(u n) {
   static u ready;if (!ready){ready=1;rts_init();}
-  *(sp = spTop) = app(app(n, _UNDEFINED), _END);
+  sp = spTop;
+  *sp = app(app(n, _UNDEFINED), _END);
   run();
 }
 |]++)
@@ -346,7 +395,7 @@ compile mods = do
     . ("};\n"++)
     . ("static const u prog[]={" ++)
     . foldr (.) id (map (\n -> shows n . (',':)) mem)
-    . ("};\nstatic u root[]={" ++)
+    . ("};\nstatic const u prog_size="++) . shows (length mem) . (";\nstatic u root[]={"++)
     . foldr (.) id (map (\(addr, _) -> shows addr . (',':)) $ elems ffes)
     . ("0};\n" ++)
     . (preamble++)
diff --git a/inn/RTS2.hs b/inn/RTS2.hs
index 54de649..d5c8c16 100644
--- a/inn/RTS2.hs
+++ b/inn/RTS2.hs
@@ -51,57 +51,78 @@ static u *mem, *altmem, *sp, *spTop, hp;
 static inline u isAddr(u n) { return n>=128; }
 static u evac(u n) {
   if (!isAddr(n)) return n;
-  u x = mem[n];
-  while (isAddr(x) && mem[x] == _T) {
-    mem[n] = mem[n + 1];
-    mem[n + 1] = mem[x + 1];
-    x = mem[n];
+  u *pn = mem + n * sizeof(u);
+  u x = *pn;
+  while (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px != _T) break;
+    u *pn1 = mem + (n + 1) * sizeof(u);
+    u *px1 = mem + (x + 1) * sizeof(u);
+    *pn = *pn1;
+    *pn1 = *px1;
+    x = *pn;
   }
-  if (isAddr(x) && mem[x] == _K) {
-    mem[n + 1] = mem[x + 1];
-    x = mem[n] = _I;
+  if (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px == _K) {
+      u *pn1 = mem + (n + 1) * sizeof(u);
+      u *px1 = mem + (x + 1) * sizeof(u);
+      *pn1 = *px1;
+      x = _I;
+      *pn = x;
+    }
   }
-  u y = mem[n + 1];
+  u *pn1 = mem + (n + 1) * sizeof(u);
+  u y = *pn1;
   switch(x) {
     case FORWARD: return y;
     case REDUCING:
-      mem[n] = FORWARD;
-      mem[n + 1] = hp;
+      *pn = FORWARD;
+      *pn1 = hp;
       hp += 2;
-      return mem[n + 1];
+      return *pn1;
     case _I:
-      mem[n] = REDUCING;
+      *pn = REDUCING;
       y = evac(y);
-      if (mem[n] == FORWARD) {
-        altmem[mem[n + 1]] = _I;
-        altmem[mem[n + 1] + 1] = y;
+      if (*pn == FORWARD) {
+        u *pa = altmem + *pn1 * sizeof(u);
+        *pa = _I;
+        pa = altmem + (*pn1 + 1) * sizeof(u);
+        *pa = y;
       } else {
-        mem[n] = FORWARD;
-        mem[n + 1] = y;
+        *pn = FORWARD;
+        *pn1 = y;
       }
-      return mem[n + 1];
+      return *pn1;
     default: break;
   }
   u z = hp;
   hp += 2;
-  mem[n] = FORWARD;
-  mem[n + 1] = z;
-  altmem[z] = x;
-  altmem[z + 1] = y;
+  *pn = FORWARD;
+  *pn1 = z;
+  u *pa = altmem + z * sizeof(u);
+  *pa = x;
+  pa = altmem + (z + 1) * sizeof(u);
+  *pa = y;
   return z;
 }
 
 static void gc() {
   hp = 128;
   u di = hp;
-  sp = altmem + TOP - 1;
-  for(u *r = root; *r; r++) *r = evac(*r);
+  sp = altmem + (TOP - 1) * sizeof(u);
+  for(u *r = root; *r; r = r + sizeof(u)) *r = evac(*r);
   *sp = evac(*spTop);
   while (di < hp) {
-    u x = altmem[di] = evac(altmem[di]);
-    di++;
-    if (x != _NUM) altmem[di] = evac(altmem[di]);
-    di++;
+    u *pd = altmem + di * sizeof(u);
+    u x = evac(*pd);
+    *pd = x;
+    di = di + 1;
+    if (x != _NUM) {
+      pd = altmem + di * sizeof(u);
+      *pd = evac(*pd);
+    }
+    di = di + 1;
   }
   spTop = sp;
   u *tmp = mem;
@@ -109,33 +130,76 @@ static void gc() {
   altmem = tmp;
 }
 
-static inline u app(u f, u x) { mem[hp] = f; mem[hp + 1] = x; return (hp += 2) - 2; }
-static inline u arg(u n) { return mem[sp [n] + 1]; }
-static inline int num(u n) { return mem[arg(n) + 1]; }
-static inline void lazy2(u height, u f, u x) {
-  u *p = mem + sp[height];
+static inline u app(u f, u x) {
+  u *p = mem + hp * sizeof(u);
   *p = f;
-  *++p = x;
-  sp += height - 1;
+  p = mem + (hp + 1) * sizeof(u);
+  *p = x;
+  hp += 2;
+  return hp - 2;
+}
+static inline u arg(u n) {
+  u *p = sp + n * sizeof(u);
+  p = mem + (*p + 1) * sizeof(u);
+  return *p;
+}
+static inline int num(u n) {
+  u a = arg(n);
+  u *p = mem + (a + 1) * sizeof(u);
+  return *p;
+}
+static inline void lazy2(u height, u f, u x) {
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  *p = f;
+  p = p + sizeof(u);
+  *p = x;
+  sp = sp + (height - 1) * sizeof(u);
   *sp = f;
 }
-static void lazy3(u height,u x1,u x2,u x3){u*p=mem+sp[height];sp[height-1]=*p=app(x1,x2);*++p=x3;*(sp+=height-2)=x1;}
-typedef unsigned long long uu;
-static inline u app64d(double d) {
-  mem[hp] = _NUM64;
-  mem[hp+1] = 0;
-  *((uu*) (mem + hp + 2)) = *((uu*) &d);
-  return (hp += 4) - 4;
+static void lazy3(u height,u x1,u x2,u x3){
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  u *ps = sp + (height - 1) * sizeof(u);
+  *ps = app(x1, x2);
+  *p = *ps;
+  p = p + sizeof(u);
+  *p = x3;
+  sp = sp + (height - 2) * sizeof(u);
+  *sp = x1;
+}
+typedef unsigned long long uu;
+static inline u app64uu(uu n) {
+  u *p = mem + hp * sizeof(u);
+  *p = _NUM64;
+  p = mem + (hp + 1) * sizeof(u);
+  *p = 0;
+  p = mem + (hp + 2) * sizeof(u);
+  uu *q = (uu*) p;
+  *q = n;
+  hp += 4;
+  return hp - 4;
+}
+static inline u app64d(uu n) { return app64uu(n); }
+static inline uu flo(u n) {
+  u a = arg(n);
+  u *p = mem + (a + 2) * sizeof(u);
+  uu *q = (uu*) p;
+  return *q;
 }
-static inline double flo(u n) { return *((double*) (mem + arg(n) + 2)); }
 static inline void lazyDub(uu n) { lazy3(4, _V, app(_NUM, n), app(_NUM, n >> 32)); }
 static inline uu dub(u lo, u hi) { return ((uu)num(hi) << 32) + (u)num(lo); }
+static inline u ite(int cond) { if (cond) return _K; return _KI; }
+static inline u fle(uu x, uu y) { return ite(x <= y); }
+static inline u feq(uu x, uu y) { return ite(x == y); }
+static inline u ule(int x, int y) { return ite((u)x <= (u)y); }
+static inline int ashr(int x, int y) { if (x < 0) return ~(~x >> y); return x >> y; }
 |]
 
 -- Main VM loop.
 comdefsrc = [r|
 F x = "foreign(num(1));"
-Y x = x "sp[1]"
+Y x = "{u *p = sp + sizeof(u); lazy2(1, arg(1), *p);}"
 Q x y z = z(y x)
 QQ f a b c d = d(c(b(a(f))))
 S x y z = x z(y z)
@@ -147,21 +211,21 @@ V x y z = z x y
 T x y = y x
 K x y = "_I" x
 KI x y = "_I" y
-I x = "sp[1] = arg(1); sp++;"
+I x = "{u *p = sp + sizeof(u); *p = arg(1); sp = sp + sizeof(u);}"
 LEFT x y z = y x
 CONS x y z w = w x y
-NUM x y = y "sp[1]"
-NUM64 x y = y "sp[1]"
-FLO x = "lazy2(1, _I, app64d((double) num(1)));"
-FLW x = "lazy2(1, _I, app64d((double) (u) num(1)));"
+NUM x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
+NUM64 x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
+FLO x = "lazy2(1, _I, app64d((u) num(1)));"
+FLW x = "lazy2(1, _I, app64d((u) num(1)));"
 OLF x = "_NUM" "((int) flo(1))"
 FADD x y = "lazy2(2, _I, app64d(flo(1) + flo(2)));"
 FSUB x y = "lazy2(2, _I, app64d(flo(1) - flo(2)));"
 FMUL x y = "lazy2(2, _I, app64d(flo(1) * flo(2)));"
 FDIV x y = "lazy2(2, _I, app64d(flo(1) / flo(2)));"
-FLE x y = "lazy2(2, _I, flo(1) <= flo(2) ? _K : _KI);"
-FEQ x y = "lazy2(2, _I, flo(1) == flo(2) ? _K : _KI);"
-PAIR64 x = "{uu n = (*((uu*) (mem + arg(1) + 2)));lazy2(1, app(_V, app(_NUM, n)), app(_NUM, n >> 32));}"
+FLE x y = "lazy2(2, _I, fle(flo(1), flo(2)));"
+FEQ x y = "lazy2(2, _I, feq(flo(1), flo(2)));"
+PAIR64 x = "{u *p = mem + (arg(1) + 2) * sizeof(u); uu *q = (uu*) p; uu n = *q; lazy2(1, app(_V, app(_NUM, n)), app(_NUM, n >> 32));}"
 DADD x y = "lazyDub(dub(1,2) + dub(3,4));"
 DSUB x y = "lazyDub(dub(1,2) - dub(3,4));"
 DMUL x y = "lazyDub(dub(1,2) * dub(3,4));"
@@ -180,19 +244,19 @@ XOR x y = "_NUM" "num(1) ^ num(2)"
 AND x y = "_NUM" "num(1) & num(2)"
 OR x y = "_NUM" "num(1) | num(2)"
 SHL x y = "_NUM" "num(1) << num(2)"
-SHR x y = "_NUM" "num(1) < 0 ? ~(~num(1) >> num(2)) : num(1) >> num(2)"
+SHR x y = "_NUM" "ashr(num(1), num(2))"
 U_SHR x y = "_NUM" "(u) num(1) >> (u) num(2)"
-EQ x y = "lazy2(2, _I, num(1) == num(2) ? _K : _KI);"
-LE x y = "lazy2(2, _I, num(1) <= num(2) ? _K : _KI);"
+EQ x y = "lazy2(2, _I, ite(num(1) == num(2)));"
+LE x y = "lazy2(2, _I, ite(num(1) <= num(2)));"
 U_DIV x y = "_NUM" "(u) num(1) / (u) num(2)"
 U_MOD x y = "_NUM" "(u) num(1) % (u) num(2)"
-U_LE x y = "lazy2(2, _I, (u) num(1) <= (u) num(2) ? _K : _KI);"
-REF x y = y "sp[1]"
+U_LE x y = "lazy2(2, _I, ule(num(1), num(2)));"
+REF x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
-WRITEREF x y z w = w "((mem[arg(2) + 1] = arg(1)), _K)" z
+WRITEREF x y z w = "{u *p = mem + (arg(2) + 1) * sizeof(u); *p = arg(1); lazy3(4, arg(4), _K, arg(3));}"
 END = "return;"
-ERR = "sp[1]=app(app(arg(1),_ERREND),_ERR2);sp++;"
+ERR = "{u *p = sp + sizeof(u); *p = app(app(arg(1), _ERREND), _ERR2); sp = sp + sizeof(u);}"
 ERR2 = "lazy3(2, arg(1), _ERROUT, arg(2));"
 ERROUT = "errchar(num(1)); lazy2(2, _ERR, arg(2));"
 ERREND = "errexit(); return;"
@@ -274,10 +338,10 @@ static int div(int a, int b) { int q = a/b; return q - (((u)(a^b)) >> 31)*(q*b!=
 static int mod(int a, int b) { int r = a%b; return r + (((u)(a^b)) >> 31)*(!!r)*b; }
 
 static void run() {
-  for(;;) {
-    if (mem + hp > sp - 8) gc();
+  while(1) {
+    if (mem + hp * sizeof(u) > sp - 8 * sizeof(u)) gc();
     u x = *sp;
-    if (isAddr(x)) *--sp = mem[x]; else switch(x) {
+    if (isAddr(x)) { u *p = mem + x * sizeof(u); sp = sp - sizeof(u); *sp = *p; } else switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ([r|
@@ -290,8 +354,8 @@ rtsAPI opts = ([r|
 void rts_init() {
   mem = malloc(TOP * sizeof(u)); altmem = malloc(TOP * sizeof(u));
   hp = 128;
-  for (u i = 0; i < sizeof(prog)/sizeof(*prog); i++) mem[hp++] = prog[i];
-  spTop = mem + TOP - 1;
+  for (u i = 0; i < prog_size; i = i + 1) { u *p = mem + hp * sizeof(u); *p = prog[i]; hp = hp + 1; }
+  spTop = mem + (TOP - 1) * sizeof(u);
 }
 |]++)
   . rtsReduce opts
@@ -401,7 +465,7 @@ compileWith topSize libc opts mods = do
     . ("};\n"++)
     . ("static const u prog[]={" ++)
     . foldr (.) id (map (\n -> shows n . (',':)) mem)
-    . ("};\nstatic u root[]={" ++)
+    . ("};\nstatic const u prog_size="++) . shows (length mem) . (";\nstatic u root[]={"++)
     . foldr (.) id (map (\(addr, _) -> shows addr . (',':)) $ elems ffes)
     . ("0};\n" ++)
     . (preamble++)
@@ -415,14 +479,14 @@ compileWith topSize libc opts mods = do
     . foldr (.) id (zipWith (\(expName, (_, ourType)) n -> ("EXPORT(f"++) . shows n . (", \""++) . (expName++) . ("\")\n"++) . genExport ourType n) (toAscList ffes) [0..])
     $ mainStr
 
-compile = compileWith "1<<24" libcHost []
+compile = compileWith "16777216" libcHost []
 
 declWarts = ([r|#define IMPORT(m,n) __attribute__((import_module(m))) __attribute__((import_name(n)));
 enum {
-  ROOT_BASE = 1<<9,  // 0-terminated array of exported functions
+  ROOT_BASE = 512,  // 0-terminated array of exported functions
   // HEAP_BASE - 4: program size
-  HEAP_BASE = (1<<20) - 128 * sizeof(u),  // program
-  TOP = 1<<22
+  HEAP_BASE = 1048576 - 128 * sizeof(u),  // program
+  TOP = 4194304
 };
 static u *root = (u*) ROOT_BASE;
 void errchar(int c) {}
@@ -432,8 +496,8 @@ void errexit() {}
 rtsAPIWarts opts = ([r|
 static inline void rts_init() {
   mem = (u*) HEAP_BASE; altmem = (u*) (HEAP_BASE + (TOP - 128) * sizeof(u));
-  hp = 128 + mem[127];
-  spTop = mem + TOP - 1;
+  { u *p = mem + 127 * sizeof(u); hp = 128 + *p; }
+  spTop = mem + (TOP - 1) * sizeof(u);
 }
 
 // Export so we can later find it in the wasm binary.
@@ -445,7 +509,8 @@ rtsReduce opts =
   . ([r|
 void rts_reduce(u n) {
   static u ready;if (!ready){ready=1;rts_init();}
-  *(sp = spTop) = app(app(n, _UNDEFINED), _END);
+  sp = spTop;
+  *sp = app(app(n, _UNDEFINED), _END);
 |]++)
   . (if "pre-post-run" `elem` opts then ("pre_run();run();post_run();"++) else ("run();"++))
   . ("\n}\n"++)
diff --git a/inn/RTSPrecisely.hs b/inn/RTSPrecisely.hs
index fd2d391..8fdb76b 100644
--- a/inn/RTSPrecisely.hs
+++ b/inn/RTSPrecisely.hs
@@ -49,7 +49,7 @@ void errexit() {}
 -- Main VM loop.
 comdefsrc = [r|
 F x = "foreign(num(1));"
-Y x = x "sp[1]"
+Y x = "{u *p = (u*)((char*)sp + sizeof(u)); lazy2(1, arg(1), *p);}"
 Q x y z = z(y x)
 S x y z = x z(y z)
 B x y z = x (y z)
@@ -60,13 +60,13 @@ V x y z = z x y
 T x y = y x
 K x y = "_I" x
 KI x y = "_I" y
-I x = "sp[1] = arg(1); sp++;"
+I x = "{u *p = (u*)((char*)sp + sizeof(u)); *p = arg(1); sp = (u*)((char*)sp + sizeof(u));}"
 LEFT x y z = y x
 CONS x y z w = w x y
-NUM x y = y "sp[1]"
-NUM64 x y = y "sp[1]"
-FLO x = "lazy2(1, _I, app64d((double) num(1)));"
-FLW x = "lazy2(1, _I, app64d((double) (u) num(1)));"
+NUM x y = "{u *p = (u*)((char*)sp + sizeof(u)); lazy2(2, arg(2), *p);}"
+NUM64 x y = "{u *p = (u*)((char*)sp + sizeof(u)); lazy2(2, arg(2), *p);}"
+FLO x = "lazy2(1, _I, app64d((u) num(1)));"
+FLW x = "lazy2(1, _I, app64d((u) num(1)));"
 OLF x = "_NUM" "((int) flo(1))"
 UUADD x y = "lazy2(2, _I, app64uu(uunum(1) + uunum(2)));"
 UUSUB x y = "lazy2(2, _I, app64uu(uunum(1) - uunum(2)));"
@@ -79,18 +79,18 @@ UUXOR x y = "lazy2(2, _I, app64uu(uunum(1) ^ uunum(2)));"
 UUSHL x y = "lazy2(2, _I, app64uu(uunum(1) << num(2)));"
 UUSHR x y = "lazy2(2, _I, app64uu(uunum(1) >> num(2)));"
 UUPAD x = "lazy2(1, _I, app64uu((u) num(1)));"
-UUEQ x y = "lazy2(2, _I, uunum(1) == uunum(2) ? _K : _KI);"
-UULE x y = "lazy2(2, _I, uunum(1) <= uunum(2) ? _K : _KI);"
+UUEQ x y = "lazy2(2, _I, ite(uunum(1) == uunum(2)));"
+UULE x y = "lazy2(2, _I, ite(uunum(1) <= uunum(2)));"
 UULO x = "_NUM" "((u) uunum(1))"
 FADD x y = "lazy2(2, _I, app64d(flo(1) + flo(2)));"
 FSUB x y = "lazy2(2, _I, app64d(flo(1) - flo(2)));"
 FMUL x y = "lazy2(2, _I, app64d(flo(1) * flo(2)));"
 FDIV x y = "lazy2(2, _I, app64d(flo(1) / flo(2)));"
-FLE x y = "lazy2(2, _I, flo(1) <= flo(2) ? _K : _KI);"
-FEQ x y = "lazy2(2, _I, flo(1) == flo(2) ? _K : _KI);"
-FFLOOR x = "lazy2(1, _I, app64d(__builtin_floor(flo(1))));"
-FSQRT x = "lazy2(1, _I, app64d(__builtin_sqrt(flo(1))));"
-PAIR64 x = "{uu n = (*((uu*) (mem + arg(1) + 2)));lazy2(1, app(_V, app(_NUM, n)), app(_NUM, n >> 32));}"
+FLE x y = "lazy2(2, _I, ite(flo(1) <= flo(2)));"
+FEQ x y = "lazy2(2, _I, ite(flo(1) == flo(2)));"
+FFLOOR x = "lazy2(1, _I, app64d(flo(1)));"
+FSQRT x = "lazy2(1, _I, app64d(flo(1)));"
+PAIR64 x = "{u a = arg(1); u *p = (u*)((char*)mem + (a + 2) * sizeof(u)); uu *q = (uu*) p; uu n = *q; lazy2(1, app(_V, app(_NUM, n)), app(_NUM, n >> 32));}"
 ADD x y = "_NUM" "num(1) + num(2)"
 SUB x y = "_NUM" "num(1) - num(2)"
 MUL x y = "_NUM" "num(1) * num(2)"
@@ -102,25 +102,25 @@ XOR x y = "_NUM" "num(1) ^ num(2)"
 AND x y = "_NUM" "num(1) & num(2)"
 OR x y = "_NUM" "num(1) | num(2)"
 SHL x y = "_NUM" "num(1) << num(2)"
-SHR x y = "_NUM" "num(1) < 0 ? ~(~num(1) >> num(2)) : num(1) >> num(2)"
+SHR x y = "_NUM" "ashr(num(1), num(2))"
 U_SHR x y = "_NUM" "(u) num(1) >> (u) num(2)"
-EQ x y = "lazy2(2, _I, num(1) == num(2) ? _K : _KI);"
-LE x y = "lazy2(2, _I, num(1) <= num(2) ? _K : _KI);"
+EQ x y = "lazy2(2, _I, ite(num(1) == num(2)));"
+LE x y = "lazy2(2, _I, ite(num(1) <= num(2)));"
 U_DIV x y = "_NUM" "(u) num(1) / (u) num(2)"
 U_MOD x y = "_NUM" "(u) num(1) % (u) num(2)"
-U_LE x y = "lazy2(2, _I, (u) num(1) <= (u) num(2) ? _K : _KI);"
-REF x y = y "sp[1]"
+U_LE x y = "lazy2(2, _I, ite((u) num(1) <= (u) num(2)));"
+REF x y = "{u *p = (u*)((char*)sp + sizeof(u)); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
-WRITEREF x y z w = w "((mem[arg(2) + 1] = arg(1)), _K)" z
+WRITEREF x y z w = "{u ref = arg(2); rts_store(ref, 1, arg(1)); lazy3(4, arg(4), _K, arg(3));}"
 END = "return 1;"
-ERR = "sp[1]=app(app(arg(1),_ERREND),_ERR2);sp++;"
+ERR = "{u *p = (u*)((char*)sp + sizeof(u)); *p = app(app(arg(1),_ERREND),_ERR2); sp = (u*)((char*)sp + sizeof(u));}"
 ERR2 = "lazy3(2, arg(1), _ERROUT, arg(2));"
 ERROUT = "errchar(num(1)); lazy2(2, _ERR, arg(2));"
 ERREND = "errexit(); return 2;"
 VMRUN = "vmrun();"
 VMPTR = "lazy3(3, arg(3), app(_NUM, arg(1)), arg(2));"
-SUSPEND = "*(sp = spTop) = app(app(arg(1), _UNDEFINED), _END); suspend_status = 0; return 1;"
+SUSPEND = "sp = spTop; *sp = app(app(arg(1), _UNDEFINED), _END); suspend_status = 0; return 1;"
 |]
 
 argList t = case t of
@@ -214,123 +214,251 @@ runFun opts ffis = ("enum{_UNDEFINED=0,"++)
   . ("};\n"++)
   . ([r|#define EXPORT(f, sym) void f() asm(sym) __attribute__((export_name(sym)));
 void *malloc(unsigned long);
+#ifdef HCC_RTS_USE_EXTERNAL_ALLOC
+void *hcc_rts_alloc(unsigned long);
+static void *rts_alloc(unsigned long n) { return hcc_rts_alloc(n); }
+#else
+static void *rts_alloc(unsigned long n) { return malloc(n); }
+#endif
 enum { FORWARD = 127, REDUCING = 126 };
 static u *mem, *altmem, *sp, *spTop, hp;
+#define HCC_RTS_ADAPTIVE_MAJOR_WORDS 83886080
+static u major_base;
+#ifdef HCC_RTS_STATS
+static u rts_gc_count, rts_major_gc_count;
+static u rts_peak_hp, rts_peak_live, rts_peak_stack, rts_peak_scratch;
+static void rts_note_hp() { if (hp > rts_peak_hp) rts_peak_hp = hp; }
+static void rts_note_live() { if (hp > rts_peak_live) rts_peak_live = hp; }
+static void rts_note_stack(u used) { if (used > rts_peak_stack) rts_peak_stack = used; }
+static void rts_note_scratch() {
+  u used = (u)(((char*)scratchpadend - (char*)scratchpad) / sizeof(u));
+  if (used > rts_peak_scratch) rts_peak_scratch = used;
+}
+static void rts_report_stats() {
+  rts_note_hp();
+  fprintf(stderr, "precisely RTS stats: gc=%u major_gc=%u peak_hp=%u peak_live=%u peak_stack=%u peak_scratch=%u top=%u adaptive_major=%u progsz=%u rootsz=%u\n",
+    rts_gc_count, rts_major_gc_count, rts_peak_hp, rts_peak_live,
+    rts_peak_stack, rts_peak_scratch, TOP, HCC_RTS_ADAPTIVE_MAJOR_WORDS, PROGSZ, ROOTSZ);
+}
+#else
+static void rts_note_hp() {}
+static void rts_note_live() {}
+static void rts_note_stack(u used) {}
+static void rts_note_scratch() {}
+static void rts_report_stats() {}
+#endif
 static inline u isAddr(u n) { return n>=128; }
 static u evac(u n) {
   if (!isAddr(n)) return n;
-  u x = mem[n];
-  while (isAddr(x) && mem[x] == _T) {
-    mem[n] = mem[n + 1];
-    mem[n + 1] = mem[x + 1];
-    x = mem[n];
+  u *p;
+  u *q;
+  p = (u*)((char*)mem + n * sizeof(u));
+  u x = *p;
+  while (isAddr(x)) {
+    p = (u*)((char*)mem + x * sizeof(u));
+    if (*p != _T) break;
+    p = (u*)((char*)mem + n * sizeof(u));
+    q = (u*)((char*)mem + (n + 1) * sizeof(u));
+    *p = *q;
+    p = (u*)((char*)mem + (x + 1) * sizeof(u));
+    *q = *p;
+    p = (u*)((char*)mem + n * sizeof(u));
+    x = *p;
   }
-  if (isAddr(x) && mem[x] == _K) {
-    mem[n + 1] = mem[x + 1];
-    x = mem[n] = _I;
+  if (isAddr(x)) {
+    p = (u*)((char*)mem + x * sizeof(u));
+    if (*p == _K) {
+      p = (u*)((char*)mem + (n + 1) * sizeof(u));
+      q = (u*)((char*)mem + (x + 1) * sizeof(u));
+      *p = *q;
+      p = (u*)((char*)mem + n * sizeof(u));
+      *p = _I;
+      x = _I;
+    }
   }
-  u y = mem[n + 1];
+  p = (u*)((char*)mem + (n + 1) * sizeof(u));
+  u y = *p;
   switch(x) {
     case FORWARD: return y;
     case REDUCING:
-      mem[n] = FORWARD;
-      mem[n + 1] = hp;
+      p = (u*)((char*)mem + n * sizeof(u));
+      *p = FORWARD;
+      p = (u*)((char*)mem + (n + 1) * sizeof(u));
+      *p = hp;
       hp += 2;
-      return mem[n + 1];
+      return *p;
     case _I:
-      mem[n] = REDUCING;
+      p = (u*)((char*)mem + n * sizeof(u));
+      *p = REDUCING;
       y = evac(y);
-      if (mem[n] == FORWARD) {
-        altmem[mem[n + 1]] = _I;
-        altmem[mem[n + 1] + 1] = y;
+      p = (u*)((char*)mem + n * sizeof(u));
+      if (*p == FORWARD) {
+        p = (u*)((char*)mem + (n + 1) * sizeof(u));
+        u w = *p;
+        p = (u*)((char*)altmem + w * sizeof(u));
+        *p = _I;
+        p = (u*)((char*)altmem + (w + 1) * sizeof(u));
+        *p = y;
       } else {
-        mem[n] = FORWARD;
-        mem[n + 1] = y;
+        p = (u*)((char*)mem + n * sizeof(u));
+        *p = FORWARD;
+        p = (u*)((char*)mem + (n + 1) * sizeof(u));
+        *p = y;
       }
-      return mem[n + 1];
+      p = (u*)((char*)mem + (n + 1) * sizeof(u));
+      return *p;
     default: break;
   }
   u z = hp;
   hp += 2;
-  mem[n] = FORWARD;
-  mem[n + 1] = z;
-  altmem[z] = x;
-  altmem[z + 1] = y;
+  p = (u*)((char*)mem + n * sizeof(u));
+  *p = FORWARD;
+  p = (u*)((char*)mem + (n + 1) * sizeof(u));
+  *p = z;
+  p = (u*)((char*)altmem + z * sizeof(u));
+  *p = x;
+  p = (u*)((char*)altmem + (z + 1) * sizeof(u));
+  *p = y;
   if (x == _NUM64) {
     hp += 2;
-    altmem[z + 2] = mem[n + 2];
-    altmem[z + 3] = mem[n + 3];
+    p = (u*)((char*)altmem + (z + 2) * sizeof(u));
+    q = (u*)((char*)mem + (n + 2) * sizeof(u));
+    *p = *q;
+    p = (u*)((char*)altmem + (z + 3) * sizeof(u));
+    q = (u*)((char*)mem + (n + 3) * sizeof(u));
+    *p = *q;
   }
   return z;
 }
 
 static u gc() {
+#ifdef HCC_RTS_STATS
+  rts_gc_count = rts_gc_count + 1;
+  rts_major_gc_count = rts_major_gc_count + 1;
+#endif
   hp = 128;
   u di = hp;
-  sp = altmem + TOP - 1;
-  for(u *r = root; r != rootend; r++) *r = evac(*r);
+  sp = (u*)((char*)altmem + (TOP - 1) * sizeof(u));
+  for(u *r = root; r != rootend; r = (u*)((char*)r + sizeof(u))) *r = evac(*r);
   *sp = evac(*spTop);
   while (di < hp) {
-    u x = altmem[di] = evac(altmem[di]);
-    di++;
+    u *p = (u*)((char*)altmem + di * sizeof(u));
+    u x = evac(*p);
+    *p = x;
+    di = di + 1;
     if (x == _NUM64) di += 2;
-    else if (x != _NUM) altmem[di] = evac(altmem[di]);
-    di++;
+    else if (x != _NUM) {
+      p = (u*)((char*)altmem + di * sizeof(u));
+      *p = evac(*p);
+    }
+    di = di + 1;
   }
   spTop = sp;
   u *tmp = mem;
   mem = altmem;
   altmem = tmp;
-  for(u usage = 0;; usage++) {
+  major_base = hp;
+  u usage = 0;
+  while(1) {
     u x = *sp;
-    if (isAddr(x)) *--sp = mem[x]; else return usage + hp + 8 >= TOP;
+    if (isAddr(x)) { u *p = (u*)((char*)mem + x * sizeof(u)); sp = (u*)((char*)sp - sizeof(u)); *sp = *p; } else {
+      rts_note_live();
+      rts_note_stack(usage);
+      return usage + hp + 8 >= TOP;
+    }
+    usage = usage + 1;
   }
 }
+static inline void rts_store(u obj, u off, u val) {
+  u *p = (u*)((char*)mem + (obj + off) * sizeof(u));
+  *p = val;
+}
 static u*global;
 u get_global(){return*global;}
 void set_global(u x){*global=x;}
 static u suspend_status;
-static inline u app(u f, u x) { mem[hp] = f; mem[hp + 1] = x; return (hp += 2) - 2; }
-static inline u arg(u n) { return mem[sp [n] + 1]; }
-static inline int num(u n) { return mem[arg(n) + 1]; }
+static inline u app(u f, u x) { u z = hp; u *p = (u*)((char*)mem + hp * sizeof(u)); *p = f; p = (u*)((char*)mem + (hp + 1) * sizeof(u)); *p = x; hp += 2; rts_note_hp(); return z; }
+static inline u arg(u n) { u *p = (u*)((char*)sp + n * sizeof(u)); p = (u*)((char*)mem + (*p + 1) * sizeof(u)); return *p; }
+static inline int num(u n) { u a = arg(n); u *p = (u*)((char*)mem + (a + 1) * sizeof(u)); return *p; }
 static inline void lazy2(u height, u f, u x) {
-  u *p = mem + sp[height];
-  *p = f;
-  *++p = x;
-  sp += height - 1;
+  u *q = (u*)((char*)sp + height * sizeof(u));
+  u obj = *q;
+  rts_store(obj, 0, f);
+  rts_store(obj, 1, x);
+  sp = (u*)((char*)sp + (height - 1) * sizeof(u));
   *sp = f;
 }
-static void lazy3(u height,u x1,u x2,u x3){u*p=mem+sp[height];sp[height-1]=*p=app(x1,x2);*++p=x3;*(sp+=height-2)=x1;}
+static void lazy3(u height,u x1,u x2,u x3){
+  u *q = (u*)((char*)sp + height * sizeof(u));
+  u obj = *q;
+  q = (u*)((char*)sp + (height - 1) * sizeof(u));
+  *q = app(x1, x2);
+  rts_store(obj, 0, *q);
+  rts_store(obj, 1, x3);
+  sp = (u*)((char*)sp + (height - 2) * sizeof(u));
+  *sp = x1;
+}
 typedef unsigned long long uu;
 static inline u app64uu(uu n) {
-  mem[hp] = _NUM64;
-  mem[hp+1] = 0;
-  *((uu*) (mem + hp + 2)) = n;
-  return (hp += 4) - 4;
+  u z = hp;
+  u *p = (u*)((char*)mem + hp * sizeof(u));
+  *p = _NUM64;
+  p = (u*)((char*)mem + (hp + 1) * sizeof(u));
+  *p = 0;
+  p = (u*)((char*)mem + (hp + 2) * sizeof(u));
+  uu *q = (uu*) p;
+  *q = n;
+  hp += 4;
+  rts_note_hp();
+  return z;
 }
-static inline u app64d(double d) { return app64uu(*((uu*) &d)); }
-static inline double flo(u n) { return *((double*) (mem + arg(n) + 2)); }
-static inline uu uunum(u n) { return *((uu*) (mem + arg(n) + 2)); }
+static inline u app64d(uu n) { return app64uu(n); }
+static inline uu flo(u n) { u a = arg(n); u *p = (u*)((char*)mem + (a + 2) * sizeof(u)); uu *q = (uu*) p; return *q; }
+static inline uu uunum(u n) { u a = arg(n); u *p = (u*)((char*)mem + (a + 2) * sizeof(u)); uu *q = (uu*) p; return *q; }
 static int div(int a, int b) { int q = a/b; return q - (((u)(a^b)) >> 31)*(q*b!=a); }
 static int mod(int a, int b) { int r = a%b; return r + (((u)(a^b)) >> 31)*(!!r)*b; }
-static inline u tagcheck(u x) { return isAddr(x) ? x&1 ? vmroot[(x - 128 - 1)/2] : x - 128 + hp : x; }
+static inline u ite(int cond) { if (cond) return _K; return _KI; }
+static inline int ashr(int x, int y) { if (x < 0) return ~(~x >> y); return x >> y; }
+static inline u tagcheck(u x) {
+  if (isAddr(x)) {
+    if (x & 1) { u *p = (u*)((char*)vmroot + ((x - 128 - 1)/2) * sizeof(u)); return *p; }
+    return x - 128 + hp;
+  }
+  return x;
+}
 void vmheap(u *start) {
   // TODO: What if there is insufficient heap?
-  u *heapptr = mem + hp;
+  u out = hp;
+  u *heapptr = (u*)((char*)mem + out * sizeof(u));
   u *p = start;
   while (p != scratchpadend) {
-    u x = *p++;
-    *heapptr++ = tagcheck(x);
-    u y = *p++;
+    u x = *p;
+    p = (u*)((char*)p + sizeof(u));
+    *heapptr = tagcheck(x);
+    out = out + 1;
+    heapptr = (u*)((char*)mem + out * sizeof(u));
+    u y = *p;
+    p = (u*)((char*)p + sizeof(u));
     if (x == _NUM64) {
-      *heapptr++ = y;
-      *heapptr++ = *p++;
-      *heapptr++ = *p++;
+      *heapptr = y;
+      out = out + 1;
+      heapptr = (u*)((char*)mem + out * sizeof(u));
+      *heapptr = *p;
+      out = out + 1;
+      heapptr = (u*)((char*)mem + out * sizeof(u));
+      p = (u*)((char*)p + sizeof(u));
+      *heapptr = *p;
+      out = out + 1;
+      heapptr = (u*)((char*)mem + out * sizeof(u));
+      p = (u*)((char*)p + sizeof(u));
     } else {
-      *heapptr++ = x == _NUM ? y : tagcheck(y);
+      if (x == _NUM) *heapptr = y; else *heapptr = tagcheck(y);
+      out = out + 1;
+      heapptr = (u*)((char*)mem + out * sizeof(u));
     }
   }
-  hp = heapptr - mem;
+  hp = out;
+  rts_note_hp();
 }
 void vmrun() {
   u x = tagcheck(num(1));
@@ -341,34 +469,47 @@ void vmrun() {
 void vmgcroot() {
   gc();
   u *p = scratchpad;
-  u sym_count = *p++;
-  while (sym_count--) *rootend++ = tagcheck(*p++);
+  u sym_count = *p;
+  p = (u*)((char*)p + sizeof(u));
+  while (sym_count) {
+    sym_count = sym_count - 1;
+    *rootend = tagcheck(*p);
+    rootend = (u*)((char*)rootend + sizeof(u));
+    p = (u*)((char*)p + sizeof(u));
+  }
   vmheap(p);
   scratchpadend = scratchpad;
 }
-void vmscratch(u n) { *scratchpadend++ = n; }
-void vmscratchroot(u n) { *scratchpadend++ = 2*n + 128 + 1; }
+void vmscratch(u n) { *scratchpadend = n; scratchpadend = (u*)((char*)scratchpadend + sizeof(u)); rts_note_scratch(); }
+void vmscratchroot(u n) { *scratchpadend = 2*n + 128 + 1; scratchpadend = (u*)((char*)scratchpadend + sizeof(u)); rts_note_scratch(); }
 |]++)
     . foldr (.) id (ffiDeclare opts <$> ffis)
     . ("static void foreign(u n) {\n  switch(n) {\n" ++)
     . foldr (.) id (zipWith ffiDefine [0..] ffis)
     . ("\n  }\n}\n" ++)
-    . ("static u step(){u kibi=1<<10;do{" ++)
+    . ("static u step(){u kibi=1024;do{" ++)
     . giantSwitch
-    . ("}while(--kibi);return 0;}\n" ++)
-    . ("static u run(){for(;;){" ++)
+    . ("kibi = kibi - 1;}while(kibi);return 0;}\n" ++)
+    . ("static u run(){while(1){" ++)
     . giantSwitch
     . ("}return 0;}\n" ++)
     . ([r|
-void run_gas(u gas) { while(!suspend_status && gas--) suspend_status = step(); }
+void run_gas(u gas) { while(!suspend_status && gas) { gas = gas - 1; suspend_status = step(); } }
 |]++)
   . rtsInit opts
   . rtsReduce opts
 
 giantSwitch = ([r|
-  if (mem + hp > sp - 8 && gc()) return 3;
+  if (hp - major_base >= HCC_RTS_ADAPTIVE_MAJOR_WORDS) {
+    rts_note_hp();
+    if (gc()) return 3;
+  }
+  if ((u*)((char*)mem + hp * sizeof(u)) > (u*)((char*)sp - 8 * sizeof(u))) {
+    rts_note_hp();
+    if (gc()) return 3;
+  }
   u x = *sp;
-  if (isAddr(x)) *--sp = mem[x]; else switch(x) {
+  if (isAddr(x)) { u *p = (u*)((char*)mem + x * sizeof(u)); sp = (u*)((char*)sp - sizeof(u)); *sp = *p; } else switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ("  }\n"++)
@@ -376,33 +517,58 @@ giantSwitch = ([r|
 rtsInit opts
   | "warts" `elem` opts = ([r|void rts_init() {
   mem = (u*) HEAP_BASE;
-  altmem = mem + TOP - 128;
-  hp = 128 + mem[127];
-  spTop = mem + TOP - 1;
-  for (rootend = root; *rootend; rootend++);
-  *(global = rootend++) = _K;
+  altmem = (u*)((char*)mem + (TOP - 128) * sizeof(u));
+  { u *p = (u*)((char*)mem + 127 * sizeof(u)); hp = 128 + *p; }
+  spTop = (u*)((char*)mem + (TOP - 1) * sizeof(u));
+  for (rootend = root; *rootend; rootend = (u*)((char*)rootend + sizeof(u)));
+  global = rootend;
+  *global = _K;
+  rootend = (u*)((char*)rootend + sizeof(u));
   vmroot = rootend;
-  scratchpadend = scratchpad = altmem + TOP;
+  scratchpadend = scratchpad = (u*)((char*)altmem + TOP * sizeof(u));
 }
 |]++)
   | otherwise = ([r|void rts_init() {
   static u done; if (done) return; done = 1;
-  mem = malloc(TOP * sizeof(u)); altmem = malloc(TOP * sizeof(u));
+  root = root8;
+  rootend = (u*)((char*)root8 + ROOTSZ * sizeof(u));
+  mem = rts_alloc(TOP * sizeof(u));
+  if (mem == 0) {
+    fputs("precisely RTS: malloc mem failed\n", stderr);
+    exit(1);
+  }
+  altmem = rts_alloc(TOP * sizeof(u));
+  if (altmem == 0) {
+    fputs("precisely RTS: malloc altmem failed\n", stderr);
+    exit(1);
+  }
+  scratchpad = rts_alloc(1048576 * sizeof(u));
+  if (scratchpad == 0) {
+    fputs("precisely RTS: malloc scratchpad failed\n", stderr);
+    exit(1);
+  }
+  scratchpadend = scratchpad;
   hp = 128;
-  unsigned char *p = (void *)rootend;
+  u *p = rootend;
   do {
     u n = 0, b = 0;
-    for(;;) {
-      u m = *p++;
+    while(1) {
+      u m = *p;
+      p = (u*)((char*)p + sizeof(u));
       n += (m & ~128) << b;
       if (m < 128) break;
       b += 7;
     }
-    mem[hp++] = n;
+    u *q = (u*)((char*)mem + hp * sizeof(u));
+    *q = n;
+    hp = hp + 1;
   } while (hp - 128 < PROGSZ);
-  spTop = mem + TOP - 1;
-  *(global = rootend++) = _K;
+  spTop = (u*)((char*)mem + (TOP - 1) * sizeof(u));
+  global = rootend;
+  *global = _K;
+  rootend = (u*)((char*)rootend + sizeof(u));
   vmroot = rootend;
+  major_base = hp;
 }
 |]++)
 
@@ -412,9 +578,12 @@ rtsReduce opts =
 |]++) else id)
   . ([r|
 void rts_reduce(u n) {
-  rts_init(), *(sp = spTop) = app(app(n, _UNDEFINED), _END);
+  rts_init();
+  sp = spTop;
+  *sp = app(app(n, _UNDEFINED), _END);
 |]++)
   . (if "pre-post-run" `elem` opts then ("pre_run();run();post_run();"++) else ("run();"++))
+  . ("rts_report_stats();"++)
   . ("\n}\n"++)
 
 -- Hash consing.
@@ -532,10 +701,10 @@ getIOType q = Left $ "main : " ++ show q
 
 libcWarts = ([r|#define IMPORT(m,n) __attribute__((import_module(m))) __attribute__((import_name(n)));
 enum {
-  ROOT_BASE = 1<<9,  // 0-terminated array of exported functions
+  ROOT_BASE = 512,  // 0-terminated array of exported functions
   // HEAP_BASE - 4: program size
-  HEAP_BASE = (1<<20) - 128 * sizeof(u),  // program
-  TOP = 1<<22
+  HEAP_BASE = 1048576 - 128 * sizeof(u),  // program
+  TOP = 4194304
 };
 static u *root = (u*) ROOT_BASE, *vmroot, *rootend, *scratchpad, *scratchpadend;
 void errchar(int c) {}
@@ -616,12 +785,13 @@ compile topSize libc opts s = do
     $ ("typedef unsigned u;\n"++)
     . ("enum{TOP="++) . (topSize++) . ("};\n"++)
     . ("enum{PROGSZ="++) . shows (length mem) . ("};\n"++)
-    . ("static unsigned char root8[]={" ++)
-    . foldr (.) id (leShows . fst <$> elems ffes)
+    . ("enum{ROOTSZ="++) . shows (size ffes) . ("};\n"++)
+    . ("static u root8[]={" ++)
+    . foldr (.) id (map (\(addr, _) -> shows addr . (", "++)) $ elems ffes)
     . foldr (.) id (leb128Shows . wordFromInt <$> mem)
     . ("};\n"++)
-    . ("static u *root = (u*)root8, *rootend = ((u*)root8) + " ++) . shows (size ffes) . (", *vmroot;\n" ++)
-    . ("u scratchpad[1<<20], *scratchpadend = scratchpad;\n" ++)
+    . ("static u *root, *rootend, *vmroot;\n" ++)
+    . ("static u *scratchpad, *scratchpadend;\n" ++)
     . (libc++)
     . runFun opts (toAscList ffis)
     . foldr (.) id (zipWith (\(expName, (_, ourType)) n -> ("EXPORT(f"++) . shows n . (", \""++) . (expName++) . ("\")\n"++) . genExport ourType n) (toAscList ffes) [0..])
diff --git a/inn/party2.hs b/inn/party2.hs
index 8d3b69f..44c4f80 100644
--- a/inn/party2.hs
+++ b/inn/party2.hs
@@ -35,4 +35,5 @@ main = getArgs >>= \case
   "type":_ -> interact $ dumpWith dumpTypes
   "wasm":opts -> interact \s -> either id id $ untangle s >>= compileWith "1<<22" libcWasm ("no-main":opts)
   "warts":opts -> interact $ either id (warts opts) . untangle
+  "top":top:_ -> interact \s -> either id id $ untangle s >>= compileWith top libcHost []
   _ -> interact \s -> either id id $ untangle s >>= compile
diff --git a/inn/precisely.hs b/inn/precisely.hs
index c364ba5..1f64a34 100644
--- a/inn/precisely.hs
+++ b/inn/precisely.hs
@@ -94,4 +94,5 @@ main = getArgs >>= \case
   "ffis":opts -> interact $ either id (($ "\n") . shows . keys) . allFFIs
   "warts":opts -> interact $ either id (warts $ "warts":opts) . allFFIs
   "wasm":opts -> interact $ either id id . compile "1<<26" libcWasm ("no-main":opts)
+  "top":top:_ -> interact $ either id id . compile top libcHost []
   _ -> interact $ either id id . compile "1<<24" libcHost []
diff --git a/party.hs b/party.hs
index b1748f4..3345da3 100644
--- a/party.hs
+++ b/party.hs
@@ -11,11 +11,11 @@ infixl 2 ||
 infixl 1 >> , >>=
 infixr 0 $
 
-foreign import ccall "putchar_shim" putChar :: Char -> IO ()
-foreign import ccall "getchar_shim" getChar :: IO Char
-foreign import ccall "eof_shim" isEOFInt :: IO Int
-foreign import ccall "getargcount" getArgCount :: IO Int
-foreign import ccall "getargchar" getArgChar :: Int -> Int -> IO Char
+ffi "putchar_shim" putChar :: Char -> IO ()
+ffi "getchar_shim" getChar :: IO Char
+ffi "eof_shim" isEOFInt :: IO Int
+ffi "getargcount" getArgCount :: IO Int
+ffi "getargchar" getArgChar :: Int -> Int -> IO Char
 
 libc = [r|#include<stdio.h>
 static int env_argc;
@@ -1479,7 +1479,7 @@ arrCount = \case
 -- Main VM loop.
 comdefsrc = [r|
 F x = "foreign(num(1));"
-Y x = x "sp[1]"
+Y x = "{u *p = sp + sizeof(u); lazy2(1, arg(1), *p);}"
 Q x y z = z(y x)
 S x y z = x z(y z)
 B x y z = x (y z)
@@ -1490,23 +1490,23 @@ V x y z = z x y
 T x y = y x
 K x y = "_I" x
 KI x y = "_I" y
-I x = "sp[1] = arg(1); sp++;"
+I x = "{u *p = sp + sizeof(u); *p = arg(1); sp = sp + sizeof(u);}"
 LEFT x y z = y x
 CONS x y z w = w x y
-NUM x y = y "sp[1]"
+NUM x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 ADD x y = "_NUM" "num(1) + num(2)"
 SUB x y = "_NUM" "num(1) - num(2)"
 MUL x y = "_NUM" "num(1) * num(2)"
 DIV x y = "_NUM" "num(1) / num(2)"
 MOD x y = "_NUM" "num(1) % num(2)"
-EQ x y = "num(1) == num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-LE x y = "num(1) <= num(2) ? lazy2(2, _I, _K) : lazy2(2, _K, _I);"
-REF x y = y "sp[1]"
+EQ x y = "if (num(1) == num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+LE x y = "if (num(1) <= num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+REF x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
-WRITEREF x y z w = w "((mem[arg(2) + 1] = arg(1)), _K)" z
+WRITEREF x y z w = "{u *p = mem + (arg(2) + 1) * sizeof(u); *p = arg(1); lazy3(4, arg(4), _K, arg(3));}"
 END = "return;"
-ERR = "sp[1]=app(app(arg(1),_ERREND),_ERR2);sp++;"
+ERR = "{u *p = sp + sizeof(u); *p = app(app(arg(1), _ERREND), _ERR2); sp = sp + sizeof(u);}"
 ERR2 = "lazy3(2, arg(1), _ERROUT, arg(2));"
 ERROUT = "errchar(num(1)); lazy2(2, _ERR, arg(2));"
 ERREND = "errexit(); return;"
@@ -1523,62 +1523,83 @@ comName i = maybe undefined id $ lookup i $ zip [1..] (fst <$> comdefs)
 preamble = [r|#define EXPORT(f, sym, n) void f() asm(sym) __attribute__((export_name(sym))); void f(){rts_reduce(root[n]);}
 void *malloc(unsigned long);
 enum { FORWARD = 127, REDUCING = 126 };
-enum { TOP = 1<<24 };
+enum { TOP = 16777216 };
 static u *mem, *altmem, *sp, *spTop, hp;
 static inline u isAddr(u n) { return n>=128; }
 static u evac(u n) {
   if (!isAddr(n)) return n;
-  u x = mem[n];
-  while (isAddr(x) && mem[x] == _T) {
-    mem[n] = mem[n + 1];
-    mem[n + 1] = mem[x + 1];
-    x = mem[n];
+  u *pn = mem + n * sizeof(u);
+  u x = *pn;
+  while (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px != _T) break;
+    u *pn1 = mem + (n + 1) * sizeof(u);
+    u *px1 = mem + (x + 1) * sizeof(u);
+    *pn = *pn1;
+    *pn1 = *px1;
+    x = *pn;
   }
-  if (isAddr(x) && mem[x] == _K) {
-    mem[n + 1] = mem[x + 1];
-    x = mem[n] = _I;
+  if (isAddr(x)) {
+    u *px = mem + x * sizeof(u);
+    if (*px == _K) {
+      u *pn1 = mem + (n + 1) * sizeof(u);
+      u *px1 = mem + (x + 1) * sizeof(u);
+      *pn1 = *px1;
+      x = _I;
+      *pn = x;
+    }
   }
-  u y = mem[n + 1];
+  u *pn1 = mem + (n + 1) * sizeof(u);
+  u y = *pn1;
   switch(x) {
     case FORWARD: return y;
     case REDUCING:
-      mem[n] = FORWARD;
-      mem[n + 1] = hp;
+      *pn = FORWARD;
+      *pn1 = hp;
       hp += 2;
-      return mem[n + 1];
+      return *pn1;
     case _I:
-      mem[n] = REDUCING;
+      *pn = REDUCING;
       y = evac(y);
-      if (mem[n] == FORWARD) {
-        altmem[mem[n + 1]] = _I;
-        altmem[mem[n + 1] + 1] = y;
+      if (*pn == FORWARD) {
+        u *pa = altmem + *pn1 * sizeof(u);
+        *pa = _I;
+        pa = altmem + (*pn1 + 1) * sizeof(u);
+        *pa = y;
       } else {
-        mem[n] = FORWARD;
-        mem[n + 1] = y;
+        *pn = FORWARD;
+        *pn1 = y;
       }
-      return mem[n + 1];
+      return *pn1;
     default: break;
   }
   u z = hp;
   hp += 2;
-  mem[n] = FORWARD;
-  mem[n + 1] = z;
-  altmem[z] = x;
-  altmem[z + 1] = y;
+  *pn = FORWARD;
+  *pn1 = z;
+  u *pa = altmem + z * sizeof(u);
+  *pa = x;
+  pa = altmem + (z + 1) * sizeof(u);
+  *pa = y;
   return z;
 }
 
 static void gc() {
   hp = 128;
   u di = hp;
-  sp = altmem + TOP - 1;
-  for(u *r = root; *r; r++) *r = evac(*r);
+  sp = altmem + (TOP - 1) * sizeof(u);
+  for(u *r = root; *r; r = r + sizeof(u)) *r = evac(*r);
   *sp = evac(*spTop);
   while (di < hp) {
-    u x = altmem[di] = evac(altmem[di]);
-    di++;
-    if (x != _NUM) altmem[di] = evac(altmem[di]);
-    di++;
+    u *pd = altmem + di * sizeof(u);
+    u x = evac(*pd);
+    *pd = x;
+    di = di + 1;
+    if (x != _NUM) {
+      pd = altmem + di * sizeof(u);
+      *pd = evac(*pd);
+    }
+    di = di + 1;
   }
   spTop = sp;
   u *tmp = mem;
@@ -1586,24 +1607,51 @@ static void gc() {
   altmem = tmp;
 }
 
-static inline u app(u f, u x) { mem[hp] = f; mem[hp + 1] = x; return (hp += 2) - 2; }
-static inline u arg(u n) { return mem[sp [n] + 1]; }
-static inline int num(u n) { return mem[arg(n) + 1]; }
-static inline void lazy2(u height, u f, u x) {
-  u *p = mem + sp[height];
+static inline u app(u f, u x) {
+  u *p = mem + hp * sizeof(u);
   *p = f;
-  *++p = x;
-  sp += height - 1;
+  p = mem + (hp + 1) * sizeof(u);
+  *p = x;
+  hp += 2;
+  return hp - 2;
+}
+static inline u arg(u n) {
+  u *p = sp + n * sizeof(u);
+  p = mem + (*p + 1) * sizeof(u);
+  return *p;
+}
+static inline int num(u n) {
+  u a = arg(n);
+  u *p = mem + (a + 1) * sizeof(u);
+  return *p;
+}
+static inline void lazy2(u height, u f, u x) {
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  *p = f;
+  p = p + sizeof(u);
+  *p = x;
+  sp = sp + (height - 1) * sizeof(u);
   *sp = f;
 }
-static void lazy3(u height,u x1,u x2,u x3){u*p=mem+sp[height];sp[height-1]=*p=app(x1,x2);*++p=x3;*(sp+=height-2)=x1;}
+static void lazy3(u height,u x1,u x2,u x3){
+  u *p = sp + height * sizeof(u);
+  p = mem + *p * sizeof(u);
+  u *ps = sp + (height - 1) * sizeof(u);
+  *ps = app(x1, x2);
+  *p = *ps;
+  p = p + sizeof(u);
+  *p = x3;
+  sp = sp + (height - 2) * sizeof(u);
+  *sp = x1;
+}
 |]
 
 runFun = ([r|static void run() {
-  for(;;) {
-    if (mem + hp > sp - 8) gc();
+  while(1) {
+    if (mem + hp * sizeof(u) > sp - 8 * sizeof(u)) gc();
     u x = *sp;
-    if (isAddr(x)) *--sp = mem[x]; else switch(x) {
+    if (isAddr(x)) { u *p = mem + x * sizeof(u); sp = sp - sizeof(u); *sp = *p; } else switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ([r|
@@ -1614,13 +1662,14 @@ runFun = ([r|static void run() {
 void rts_init() {
   mem = malloc(TOP * sizeof(u)); altmem = malloc(TOP * sizeof(u));
   hp = 128;
-  for (u i = 0; i < prog_size; i++) mem[hp++] = prog[i];
-  spTop = mem + TOP - 1;
+  for (u i = 0; i < prog_size; i = i + 1) { u *p = mem + hp * sizeof(u); *p = prog[i]; hp = hp + 1; }
+  spTop = mem + (TOP - 1) * sizeof(u);
 }
 
 void rts_reduce(u n) {
   static u ready;if (!ready){ready=1;rts_init();}
-  *(sp = spTop) = app(app(n, _UNDEFINED), _END);
+  sp = spTop;
+  *sp = app(app(n, _UNDEFINED), _END);
   run();
 }
 |]++)
diff --git a/party_shims.c b/party_shims.c
new file mode 100644
index 0000000..eb2b082
--- /dev/null
+++ b/party_shims.c
@@ -0,0 +1,34 @@
+/* Upstream blynn/compiler libc shims, minus the env_argv/getargcount/getargchar
+ * that orians' methodically already emits as part of its own libc preamble.
+ *
+ * These are the foreign-call targets named by upstream party.hs's ffi
+ * declarations (after rewriting `foreign import ccall` -> `ffi`). They
+ * implement upstream's stdin-with-EOF-lookahead protocol that upstream's
+ * Haskell code expects.
+ */
+
+#include <stdio.h>
+
+static int nextCh, isAhead;
+
+int eof_shim(void) {
+  if (!isAhead) {
+    isAhead = 1;
+    nextCh = getchar();
+  }
+  return nextCh == -1;
+}
+
+void exit(int);
+
+void putchar_shim(int c) { putchar(c); }
+
+int getchar_shim(void) {
+  if (!isAhead) nextCh = getchar();
+  if (nextCh == -1) exit(1);
+  isAhead = 0;
+  return nextCh;
+}
+
+void errchar(int c) { fputc(c, stderr); }
+void errexit(void) { fputc('\n', stderr); }
