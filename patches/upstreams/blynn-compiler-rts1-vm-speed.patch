diff --git a/inn/RTS1.hs b/inn/RTS1.hs
--- a/inn/RTS1.hs
+++ b/inn/RTS1.hs
@@ -166,20 +166,20 @@
 comdefsrc = [r|
 F x = "foreign(num(1));"
 Y x = "{u *p = sp + sizeof(u); lazy2(1, arg(1), *p);}"
-Q x y z = z(y x)
-S x y z = x z(y z)
-B x y z = x (y z)
-BK x y z = x y
-C x y z = x z y
-R x y z = y z x
-V x y z = z x y
-T x y = y x
-K x y = "_I" x
-KI x y = "_I" y
-I x = "{u *p = sp + sizeof(u); *p = arg(1); sp = sp + sizeof(u);}"
+Q x y z = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); u xa = *p; p = sp + 2 * sizeof(u); p = mem + (*p + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xb; h = h + sizeof(u); *h = xa; *t = xc; *t1 = hp; hp = hp + 2; sp = q - sizeof(u); *sp = xc; x = xc; goto run_check;}"
+S x y z = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); p = mem + (*p2 + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xb; h = h + sizeof(u); *h = xc; h = h + sizeof(u); *h = xa; h = h + sizeof(u); *h = xc; u ap = hp + 2; *p2 = ap; *t = ap; *t1 = hp; hp = hp + 4; sp = p1; *sp = xa; x = xa; goto run_check;}"
+B x y z = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); u xa = *p; p = sp + 2 * sizeof(u); p = mem + (*p + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xb; h = h + sizeof(u); *h = xc; *t = xa; *t1 = hp; hp = hp + 2; sp = q - sizeof(u); *sp = xa; x = xa; goto run_check;}"
+BK x y z = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); u xa = *p; p = sp + 2 * sizeof(u); p = mem + (*p + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); *t = xa; t = t + sizeof(u); *t = xb; sp = q - sizeof(u); *sp = xa; x = xa; goto run_nogc;}"
+C x y z = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); p = mem + (*p2 + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xa; h = h + sizeof(u); *h = xc; *p2 = hp; *t = hp; *t1 = xb; hp = hp + 2; sp = p1; *sp = xa; x = xa; goto run_check;}"
+R x y z = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); p = mem + (*p2 + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xb; h = h + sizeof(u); *h = xc; *p2 = hp; *t = hp; *t1 = xa; hp = hp + 2; sp = p1; *sp = xb; x = xb; goto run_check;}"
+V x y z = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); p = mem + (*p2 + 1) * sizeof(u); u xb = *p; u *q = sp + 3 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xc = *t1; u *h = mem + hp * sizeof(u); *h = xc; h = h + sizeof(u); *h = xa; *p2 = hp; *t = hp; *t1 = xb; hp = hp + 2; sp = p1; *sp = xc; x = xc; goto run_check;}"
+T x y = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xb = *t1; *t = xb; *t1 = xa; sp = p1; *sp = xb; x = xb; goto run_nogc;}"
+K x y = "{u *p1 = sp + sizeof(u); u *p = mem + (*p1 + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); *t = _I; t = t + sizeof(u); *t = xa; sp = p1; *sp = _I; x = _I; goto run_nogc;}"
+KI x y = "{u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); *t = _I; sp = sp + sizeof(u); *sp = _I; x = _I; goto run_nogc;}"
+I x = "{u *p = sp + sizeof(u); u *q = mem + (*p + 1) * sizeof(u); u v = *q; *p = v; sp = p; x = v; goto run_nogc;}"
 LEFT x y z = y x
-CONS x y z w = w x y
-NUM x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
+CONS x y z w = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); u xa = *p; u *p2 = sp + 2 * sizeof(u); p = mem + (*p2 + 1) * sizeof(u); u xb = *p; u *p3 = sp + 3 * sizeof(u); u *q = sp + 4 * sizeof(u); u n = *q; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u xd = *t1; u *h = mem + hp * sizeof(u); *h = xd; h = h + sizeof(u); *h = xa; *p3 = hp; *t = hp; *t1 = xb; hp = hp + 2; sp = p2; *sp = xd; x = xd; goto run_check;}"
+NUM x y = "{u *p1 = sp + sizeof(u); u xn = *p1; u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); u f = *t1; *t = f; *t1 = xn; sp = p1; *sp = f; x = f; goto run_nogc;}"
 ADD x y = "_NUM" "num(1) + num(2)"
 SUB x y = "_NUM" "num(1) - num(2)"
 MUL x y = "_NUM" "num(1) * num(2)"
@@ -190,8 +190,8 @@
 XOR x y = "_NUM" "num(1) ^ num(2)"
 AND x y = "_NUM" "num(1) & num(2)"
 OR x y = "_NUM" "num(1) | num(2)"
-EQ x y = "if (num(1) == num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
-LE x y = "if (num(1) <= num(2)) lazy2(2, _I, _K); else lazy2(2, _K, _I);"
+EQ x y = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); p = mem + (*p + 1) * sizeof(u); u a = *p; u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); p = mem + (*t1 + 1) * sizeof(u); u b = *p; u r = _KI; if (a == b) r = _K; *t = _I; *t1 = r; sp = sp + sizeof(u); *sp = _I; x = _I; goto run_nogc;}"
+LE x y = "{u *p = sp + sizeof(u); p = mem + (*p + 1) * sizeof(u); p = mem + (*p + 1) * sizeof(u); int a = *p; u *p2 = sp + 2 * sizeof(u); u n = *p2; u *t = mem + n * sizeof(u); u *t1 = t + sizeof(u); p = mem + (*t1 + 1) * sizeof(u); int b = *p; u r = _KI; if (a <= b) r = _K; *t = _I; *t1 = r; sp = sp + sizeof(u); *sp = _I; x = _I; goto run_nogc;}"
 REF x y = "{u *p = sp + sizeof(u); lazy2(2, arg(2), *p);}"
 NEWREF x y z = z ("_REF" x) y
 READREF x y z = z "num(1)" y
@@ -275,15 +275,46 @@
 comEnum s = maybe (error s) id $ lookup s $ zip (fst <$> comdefs) [1..]
 comName i = maybe undefined id $ lookup i $ zip [1..] (fst <$> comdefs)
-
+
+hotDispatch = ["B","I","S","C","R","NUM","BK","T","Q","KI","CONS","K","EQ","V"]
+
+findComb name = maybe undefined id $ find ((name ==) . fst) comdefs
+
+genHotComb name = case findComb name of
+  (_, (_, E (StrCon body))) -> ("if (x == _"++) . (name++) . (") "++) . (body++) . ('\n':)
+
 runFun = ([r|
 static int div(int a, int b) { int q = a/b; return q - (((u)(a^b)) >> 31)*(q*b!=a); }
 static int mod(int a, int b) { int r = a%b; return r + (((u)(a^b)) >> 31)*(!!r)*b; }
-
+
 static void run() {
+  u x;
+  u *lim;
   while(1) {
-    if (mem + hp * sizeof(u) > sp - 8 * sizeof(u)) gc();
-    u x = *sp;
-    if (isAddr(x)) { u *p = mem + x * sizeof(u); sp = sp - sizeof(u); *sp = *p; } else switch(x) {
+    x = *sp;
+    run_check:
+    lim = mem + (hp + 8) * sizeof(u);
+    if (lim > sp) {
+      gc();
+      lim = mem + (hp + 8) * sizeof(u);
+      x = *sp;
+    }
+    run_nogc:
+    if (x >= 128) {
+      do {
+        u *p = mem + x * sizeof(u);
+        sp = sp - sizeof(u);
+        x = *p;
+        *sp = x;
+        if (sp < lim) {
+          gc();
+          lim = mem + (hp + 8) * sizeof(u);
+          x = *sp;
+        }
+      } while (x >= 128);
+    }
+|]++)
+  . foldr (.) id (genHotComb <$> hotDispatch)
+  . ([r|    switch(x) {
 |]++)
   . foldr (.) id (genComb <$> comdefs)
   . ([r|
